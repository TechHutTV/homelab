{
  "input": {
    "raw_file_manifest": {
      "type": "array",
      "description": "List of raw media files from the production session",
      "items": {
        "type": "object",
        "properties": {
          "file_name": {"type": "string"},
          "file_type": {"type": "string", "enum": ["video", "audio", "photo", "b_roll", "asset"]},
          "file_size_mb": {"type": "number"},
          "file_path": {"type": "string"}
        },
        "required": ["file_name", "file_type", "file_path"]
      }
    },
    "project_id": {
      "type": "string",
      "description": "ClickUp project/task ID for this production"
    },
    "client_name": {
      "type": "string",
      "description": "Client name used in file naming and channel selection"
    },
    "project_type": {
      "type": "string",
      "description": "Type of project (e.g., BrandVideo, SocialContent, EventCoverage, ProductShoot)"
    },
    "edit_notes": {
      "type": "object",
      "description": "Creative direction and editing instructions for Sparrow",
      "properties": {
        "creative_direction": {"type": "string"},
        "mood_tone": {"type": "string"},
        "specific_instructions": {"type": "string"},
        "deadline": {"type": "string", "format": "date"},
        "priority": {"type": "string", "enum": ["normal", "high", "rush"]}
      }
    },
    "channel_override": {
      "type": "string",
      "description": "Optional override for the Lark channel (bypasses default channel selection)"
    }
  },
  "output": {
    "google_drive_folder_url": {
      "type": "string",
      "description": "Shareable URL for the organized Google Drive folder"
    },
    "lark_message_id": {
      "type": "string",
      "description": "ID of the handoff notification message posted to Lark"
    },
    "lark_channel": {
      "type": "string",
      "description": "The Lark channel where the notification was posted"
    },
    "acknowledgment_status": {
      "type": "object",
      "description": "Sparrow team acknowledgment tracking",
      "properties": {
        "acknowledged": {"type": "boolean"},
        "acknowledged_by": {"type": "string"},
        "acknowledged_at": {"type": "string", "format": "date-time"}
      }
    },
    "clickup_task_status": {
      "type": "string",
      "description": "Updated ClickUp task status after handoff",
      "enum": ["in_editing", "handoff_failed"]
    }
  }
}
